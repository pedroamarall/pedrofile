project_id: '%CROWDIN_PROJECT_ID%'
api_token: '%CROWDIN_PERSONAL_TOKEN%' # Ou '%CROWDIN_API_TOKEN%' se esse for o nome do seu segredo no GitHub

files:
  - source: /locales/en/*.json
    translation: /locales/%two_letters_code%/%original_file_name%

bundles:
  - 1